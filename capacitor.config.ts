import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tuto-ionic-react-firebase',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
